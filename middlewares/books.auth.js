function auth(req, res, next) {
    let isVererified = true;
    if (isVerified) {
        console.log("added successfully")

        next()
    } else {
        console.log("not authorrised")
        throw error;
    }

}
module.exports=auth    
